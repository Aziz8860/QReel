// Determine base width and height for responsiveness
export const heightMobileUI = 800;
export const widthMobileUI = 360;

export const scan_product_option = {
    qr: "QR",
    camera: "CAMERA"
}

import theme, { COLORS, SIZES, FONTS, lightTheme, darkTheme } from "./theme";
export {
    theme,
    COLORS,
    SIZES,
    FONTS,
    lightTheme,
    darkTheme,
}